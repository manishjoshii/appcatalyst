apiVersion: apps/v1
kind: Deployment
metadata:
  name: acs-proxy
  namespace: acs-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: acs-proxy
  template:
    metadata:
      labels:
        app: acs-proxy
    spec:
      containers:
        - name: proxy
          image: acs-proxy:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          env:
            - name: REDIS_URL
              # Refers to the service in the same namespace
              value: "redis://redis-service:6379"
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: acs-proxy-svc
  namespace: acs-apps
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: acs-proxy
