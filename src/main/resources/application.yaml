spring:
  application:
    name: appcatalyst

  datasource:
    url: jdbc:postgresql://localhost:5432/appcatalyst
    username: postgres
    password: '0000'
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
